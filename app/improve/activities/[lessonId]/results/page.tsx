/**
 * @fileoverview Practice results page for a specific lesson.
 * Displays phase scores, composite score, and mastery status.
 */

import PracticeResultsContent from '@/components/practice/PracticeResultsContent';

export default function PracticeLessonResultsPage() {
  return <PracticeResultsContent />;
}

