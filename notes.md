# Notes

> **Important:** Human only file, no AI edits.

- Uses Vite with React + Tailwind on AWS Amplify, an express server using AWS AppRunner, and a PostgreSQL database with Drizzle ORM on AWS RDS. Using better-auth
- Stay on dev branch, Asher will set up main deployment when we need a full production setup